#include <stdio.h>

/**
 * main - escribe el nombre del atributo
 *@argc:numero de argumentos
 *@argv:argumento de programa
 * Return: devuelve el resultado
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
