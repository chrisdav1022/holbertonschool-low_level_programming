#include "holberton.h"
/**
 * _isdigit - print 1 if is digit
 * @c: contains digit 1 and 0 if is alpha
 * Return: return 0 and 1
 */
int _isdigit(int c)
{
	if (c >= 'a')
		return (0);

	return (1);
}
